{
  "name": "@senastr/agent-runtime",
  "version": "0.1.0",
  "private": true,
  "description": "senastr agent loop: provider-agnostic model streaming + tool orchestration over a HostBridge",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@senastr/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "~5.9.3"
  }
}
