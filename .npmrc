# Electron's postinstall downloads a ~100MB binary; keep pnpm's build-script
# gate honest about which packages are allowed to run one.
onlyBuiltDependencies[]=electron
onlyBuiltDependencies[]=esbuild
